import { KeyDetectDirective } from './key-detect.directive';

describe('KeyDetectDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyDetectDirective();
    expect(directive).toBeTruthy();
  });
});
